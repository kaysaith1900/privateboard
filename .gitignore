# deps + builds
node_modules
dist
dist-electron
release
*.tsbuildinfo

# logs / coverage
*.log
coverage
.nyc_output

# editor / OS
.DS_Store
.idea
.vscode
*.swp
.claude/

# local user state (never commit)
.boardroom-local
~/.boardroom

# env
.env
.env.*
!.env.example

# npm pack output
*.tgz

# Netlify build output (assembled from public/ at deploy time)
_site/

# Skill packs · `skills-lock.json` is the committed lockfile (like
# package-lock); `.agents/` is the populated cache the lockfile
# resolves to and shouldn't be tracked.
.agents/

# Design / avatar exploration previews — generated artifacts, not source.
/abstract-pets-gallery.svg
/brick-variants-gallery.svg
/director-avatar-demo.svg
/director-gallery.svg
/director-v2.svg
/seed-avatar-preview.svg
/stylish-gallery.svg
